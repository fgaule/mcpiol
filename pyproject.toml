[project]
name = "mcpiol"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "cachetools>=5.5.2",
    "httpx>=0.28.1",
    "mcp[cli]>=1.3.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.pyright]
strict = ["."]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "pytest-cov>=6.2.1",
    "pyright>=1.1.0",
]
